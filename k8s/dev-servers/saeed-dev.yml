apiVersion: v1
kind: Namespace
metadata:
  name: development
---
apiVersion: v1
kind: Service
metadata:
 name: saeed-dev
 namespace: development
spec:
 ports:
 - port: 3000
   targetPort: 3000
 selector:
   app: saeed-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: saeed-dev
  name: saeed-dev
  namespace: development
spec:
  selector:
    matchLabels:
      app: saeed-dev
  replicas: 1
  template:
    metadata:
      labels:
        app: saeed-dev
    spec:
      nodeSelector:
        role: worker-lg
      containers:
      - image: litcodes/theia:latest
        imagePullPolicy: Always
        name: saeed-dev
        ports:
        - containerPort: 3000
        volumeMounts:
          - mountPath: /home/project
            name: project-dir 
      volumes:
        - name: project-dir
          persistentVolumeClaim:
            claimName: saeed-dev-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: saeed-dev-pvc
  namespace: development
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 15Gi
